# CXsyncSRtest
Windows diagnostic that verifies whether a workstation can upload recordings from the Screen Recording desktop app. Checks DNS, TCP 443, TLS inspection, HTTPS PUT, WebSocket upgrade, local recording backlog, autostart, disk, log errors, clock skew, and session freshness. Single self-contained .exe (~54 KB), no install.
